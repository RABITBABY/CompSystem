<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cs.dao.article.ArticleMapper" >
  <resultMap id="BaseResultMap" type="com.cs.pojo.Article" >
  </resultMap>
  
	
 <select id="getMtypeList" resultType="Map" parameterType="Map">
   select ar.* ,ad.adminName adminName from competition.article ar,competition.administer ad 
   where ar.pubuserno=ad.adminno and ar.articleType=#{type} ORDER BY ar.pubDate desc limit #{page},#{pageSize}
   </select>

 <select id="getTotal" resultType="int" parameterType="String">
  select count(*) from article  where articleType=#{type} 
</select>

 <select id="findById" resultType="Map" parameterType="int">
  select ar.* ,ad.adminName adminName from competition.article ar,competition.administer ad 
   where ar.pubuserno=ad.adminno and ar.articleid=#{id}
</select>

<insert id="insertArticle" parameterType="com.cs.pojo.Article">
    insert into article ( title, pubDate, 
      pubUserNo, articleType, relationId,  content)
    values ( #{title}, #{pubdate}, 
      #{pubuserno}, #{articletype}, #{relationId}, 
      #{content})

</insert>
<delete id="deleteArticle" parameterType="int">
	DELETE  from  article where articleId=#{articleId}
</delete>

<update id="updateArticle" parameterType="Map">
	update article set title=#{title},content=#{content},pubDate=SYSDATE()  where articleId=#{articleId}

</update>
</mapper>