<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:websocket="http://www.springframework.org/schema/websocket"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/websocket http://www.springframework.org/schema/websocket/spring-websocket.xsd">

    <websocket:message-broker application-destination-prefix="/app">
        <websocket:stomp-endpoint path="/hello">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/admin">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/sb">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/waiter">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/cooker">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/themanager">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/cashier">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:stomp-endpoint path="/fresh">
            <websocket:sockjs/>
        </websocket:stomp-endpoint>
        <websocket:simple-broker prefix="/topic"/>
    </websocket:message-broker>
</beans>